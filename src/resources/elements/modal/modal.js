import {bindable, bindingMode} from 'aurelia-framework';

export class Modal {
  @bindable idModal = "";
  @bindable value;
  // valueChanged(newValue, oldValue) {

  // }
  constructor(){
    
  }
}

